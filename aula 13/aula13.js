//               0123456789... -> indexadas, índice 0 é o início
let umaString = "O rato roeu a roupa do rei de Roma."; // barra invertida é usada como 'escape' para utilizar aspas duplas mais de uma vez.

console.log(umaString.toUpperCase());
console.log(umaString.toLowerCase());

// (split) divisão
// (slice) (substring) fatiamento
// (concat) (charAt) exibimos o índice de acordo com a numeração usando '[]'; ex: console.log(umaString[4]); -> Exibe 'e';
// <- template strings concatenação
